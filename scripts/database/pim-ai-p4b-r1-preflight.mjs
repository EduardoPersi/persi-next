import assert from "node:assert/strict";
import {mkdir,readFile,writeFile} from "node:fs/promises";
import {existsSync,readFileSync} from "node:fs";
import {resolve} from "node:path";
import postgres from "postgres";
import {preparePimAiDryRun} from "../../lib/pim/ai-dry-run.ts";
import {GPT_5_4_MINI_PRICING} from "../../lib/pim/ai-pricing.ts";
import {PimAttributeExtractor} from "../../lib/pim/extractor.ts";
import {auditSemanticUnit} from "../../lib/pim/semantic-validation.ts";
import {buildSafeAiProductContext} from "../../lib/pim/safe-ai-context.ts";
import {auditForbiddenSafeContext,buildPilotSourceSnapshot,createStableFingerprint,safeContextFingerprint,scanPromptInjection,scanSafeContextDlp} from "../../lib/pim/pilot-preparation.ts";

if(process.argv[2]!=="--staging-read-only"||process.argv.length!==3)throw new Error("Exige exatamente --staging-read-only.");
process.env.PIM_AI_ENABLED="false";const PROJECT_REF="vtrujmhhkmvjzfklzxip",root=resolve("."),base=resolve(root,"supabase/.temp/pim-ai/p4b-r1"),manifest=JSON.parse(await readFile(resolve(root,"supabase/.temp/pim-ai/p4a/pilot-manifest.json"),"utf8")),originalResult=await readFile(resolve(root,"supabase/.temp/pim-ai/p4b/p4b-results.json"),"utf8"),originalMarker=await readFile(resolve(root,"supabase/.temp/pim-ai/p4b/p4b-a.json"),"utf8"),offlineAudit=JSON.parse(await readFile(resolve(root,"supabase/.temp/pim-ai/p4b/p4b-a-offline-reconciliation.json"),"utf8"));
assert.equal(JSON.parse(originalMarker).state,"COMPLETED");assert.equal(offlineAudit.finalPipelineSafetyAfterFix,"PASS");assert.equal(offlineAudit.reconciliation.unsupportedFacts.length,0);assert.equal(offlineAudit.reconciliation.blockingConflicts.length,0);
const originalHashes={marker:createStableFingerprint(originalMarker),results:createStableFingerprint(originalResult)};
function secret(name){for(const path of [".env.staging.local",".env.local"]){if(!existsSync(path))continue;const line=readFileSync(path,"utf8").split(/\r?\n/).find(item=>item.startsWith(`${name}=`));if(line)return line.slice(name.length+1).trim().replace(/^(['"])(.*)\1$/,"$2");}throw new Error(`${name} não configurada.`);}
const poolerTemplate=readFileSync("supabase/.temp/pooler-url","utf8").trim();assert.ok(poolerTemplate.includes(PROJECT_REF));const url=new URL(poolerTemplate);url.password=secret("PERSI_STAGING_DB_PASSWORD");const sql=postgres(url.toString(),{max:1,prepare:false,ssl:"require",connect_timeout:20,idle_timeout:5});
const compact=value=>typeof value==="string"?value.replace(/\s+/g," ").trim():value;
const toInput=row=>({productId:row.id,name:compact(row.name),description:compact(row.description)||compact(row.short_description)||null,sku:row.sku||"",gtin:row.gtin||null,brand:compact(row.brand)||null,category:compact(row.category)||null,attributes:Array.isArray(row.attributes)?row.attributes.map(item=>({name:compact(item.name),value:compact(item.value)})):[]});
async function reload(id){return sql.begin(async tx=>{await tx`set transaction read only`;const [identity]=await tx`select current_database() database,current_setting('transaction_read_only') "readOnly"`;assert.equal(identity.database,"postgres");assert.equal(identity.readOnly,"on");const [row]=await tx`select p.id,p.name,p.short_description,p.description,b.name brand,coalesce(c.name,pcat.name) category,v.sku,v.gtin,coalesce(a.items,'[]'::jsonb) attributes from products p join lateral(select x.sku,x.gtin from product_variants x where x.product_id=p.id order by x.created_at,x.id limit 1)v on true left join brands b on b.id=p.brand_id left join categories c on c.id=p.primary_category_id left join lateral(select c2.name from product_categories pc join categories c2 on c2.id=pc.category_id where pc.product_id=p.id order by c2.name,c2.id limit 1)pcat on true left join lateral(select jsonb_agg(jsonb_build_object('name',z.name,'value',z.display_value) order by z.name,z.display_value) items from(select distinct at.name,av.display_value from product_attribute_values pav join attributes at on at.id=pav.attribute_id join attribute_values av on av.id=pav.attribute_value_id where pav.product_id=p.id)z)a on true where p.id=${id}::uuid`;assert.ok(row);return toInput(row);});}
const config={enabled:false,provider:"openai",model:"gpt-5.4-mini",promptVersion:"pim-enrichment-v2",apiKey:null,maxOutputTokens:1200,timeoutMs:20000,rateLimitPerMinute:2,maxEstimatedCostUsdMicros:20000n,pricing:GPT_5_4_MINI_PRICING,includeGtin:false},extractor=new PimAttributeExtractor(),products=[];
for(const historic of manifest.products.slice(1)){
 const input=await reload(historic.localProductReference),source=buildPilotSourceSnapshot(input),safe=buildSafeAiProductContext(input,{includeGtin:false}),sourceHash=createStableFingerprint(source),safeHash=safeContextFingerprint(safe),sourceStale=sourceHash!==historic.sourceFingerprint,safeChanged=safeHash!==historic.safeContextFingerprint,dlp=scanSafeContextDlp(safe),injection=scanPromptInjection(input),forbidden=auditForbiddenSafeContext(safe),candidates=extractor.extract(source),semanticMismatches=candidates.map(item=>auditSemanticUnit(item.attribute,item.value)).filter(Boolean),conflicts=candidates.filter(item=>item.status==="CONFLICT"),dry=preparePimAiDryRun(input,config);
 if(sourceStale||dlp.status!=="PASS"||injection.status!=="PASS"||forbidden.length||semanticMismatches.length||conflicts.length)console.log(JSON.stringify({diagnostic:{profile:historic.profile,sourceStale,dlp:dlp.status,injection:injection.status,forbidden,semanticMismatches,conflicts:conflicts.map(item=>({attribute:item.attribute,value:item.value,conflictingValues:item.conflictingValues}))}}));
 assert.equal(sourceStale,false);assert.equal(dlp.status,"PASS");assert.equal(injection.status,"PASS");assert.deepEqual(forbidden,[]);assert.equal(semanticMismatches.length,0);assert.equal(conflicts.length,0);assert.equal(dry.requestSent,false);
 if(historic.profile==="B_ELECTRICAL"){assert.ok(candidates.some(item=>item.attribute==="current"&&item.value==="25A"));assert.ok(candidates.some(item=>item.attribute==="voltage"&&item.value==="600V"));assert.equal(candidates.some(item=>item.attribute==="power"),false);}
 if(historic.profile==="C_SIMPLE")assert.deepEqual(candidates.map(item=>item.attribute),["brand"]);
 if(historic.profile==="D_RICH_DATA"){assert.ok(candidates.some(item=>item.attribute==="color"&&item.value.toLocaleLowerCase("pt-BR")==="branco"));assert.equal(candidates.some(item=>/cai x a/i.test(item.value)),false);}
 if(historic.profile==="E_INCOMPLETE"){assert.ok(candidates.some(item=>item.value==="10mm"));assert.equal(candidates.some(item=>/C12|20\s*Peças/i.test(item.value)),false);}
 products.push({pilotId:`P4B-R1-${historic.profile[0]}`,profile:historic.profile,localProductReference:historic.localProductReference,sourceFingerprintP4a:historic.sourceFingerprint,sourceFingerprintR1:sourceHash,sourceStale,safeContextFingerprintP4a:historic.safeContextFingerprint,safeContextFingerprintR1:safeHash,safeContextChanged:safeChanged,diff:safeChanged?"STRUCTURED_DIFF_REQUIRED":[],diffClassification:safeChanged?"UNEXPECTED_CHANGE":"NO_CHANGE",materialChange:false,dlp:dlp.status,forbiddenFields:forbidden,promptInjectionScan:injection.status,deterministicAttributes:candidates,semanticMismatches,conflicts,request:{built:true,sent:false,estimatedInputTokens:dry.scope.estimatedInputTokens,estimatedMaxCostUsdMicros:dry.budget.estimatedCostUsdMicros.toString()},pilotEligibleR1:true});
}
await sql.end({timeout:5});assert.equal(createStableFingerprint(await readFile(resolve(root,"supabase/.temp/pim-ai/p4b/p4b-a.json"),"utf8")),originalHashes.marker);assert.equal(createStableFingerprint(await readFile(resolve(root,"supabase/.temp/pim-ai/p4b/p4b-results.json"),"utf8")),originalHashes.results);
const estimated=products.reduce((sum,item)=>sum+BigInt(item.request.estimatedMaxCostUsdMicros),0n),recommended=40000n;assert.ok(estimated<=recommended);assert.ok(recommended<=45041n);
const report={phase:"P.4-B-R1-PREFLIGHT",createdAt:new Date().toISOString(),p4bA:{markerPreserved:true,responsePreserved:true,offlineRegression:"PASS",quality:"50/50",additionalRequests:0},target:{name:"persi-staging",projectRef:PROJECT_REF,readOnly:true},model:"gpt-5.4-mini",promptVersion:"pim-enrichment-v2",maxOutputTokens:1200,estimatedCostUsdMicros:estimated.toString(),remainingOriginalBudgetUsdMicros:"45041",recommendedHardBudgetUsdMicros:recommended.toString(),realRequestAttempts:0,realAiCalls:0,realAiCostUsdMicros:"0",futureMarkers:["p4b-r1-b.json","p4b-r1-c.json","p4b-r1-d.json","p4b-r1-e.json"],products};
await mkdir(base,{recursive:true});await writeFile(resolve(base,"preflight-manifest.json"),`${JSON.stringify(report,null,2)}\n`,"utf8");console.log(JSON.stringify({preflight:"PASS",products:4,eligible:products.filter(item=>item.pilotEligibleR1).length,sourceStale:products.filter(item=>item.sourceStale).length,safeContextChanged:products.filter(item=>item.safeContextChanged).length,dlp:"4/4",conflicts:0,estimatedCostUsdMicros:estimated.toString(),recommendedHardBudgetUsdMicros:recommended.toString(),realRequests:0,manifest:"supabase/.temp/pim-ai/p4b-r1/preflight-manifest.json"},null,2));
